# SmartStitch 测试与开发计划

## 1. 开发阶段

### 阶段一：配置与素材预检

- 建立 Python 项目、配置模型和注释模板。
- 实现路径解析、素材扫描、过滤和 ffprobe 缓存。
- 实现配置校验和预检报告。
- 实现 schema 1 `benefit_video` 到 schema 2 `benefit_1` 的无损加载迁移，以及动态利益点段校验。
- 单元测试覆盖中文路径、空格路径、`._` 文件、缺失目录和异常媒体。

交付判断：给定当前素材目录，可稳定列出有效素材及媒体规格，不写任何成片。

### 阶段二：组合计划与权重

- 实现配额洗牌、独立随机、随机种子和组合去重。
- 使计划器和去重签名动态包含所有启用的 `benefit_<n>` 素材组。
- 实现产品标签关联。
- 输出计划预览、理论/实际分布和 JSON 清单。

交付判断：相同输入和种子计划可复现，权重统计符合规则。

### 阶段三：FFmpeg 渲染

- 实现视频/音频标准化。
- 实现时间线 concat、静态/视频尾帧、透明图片最高层 overlay。
- 实现临时文件、结果探测、错误分类和重试。

交付判断：用小型测试素材完成端到端渲染，成片要素、时长、音画和叠图正确。

### 阶段四：后端任务系统

- 实现 API、SQLite、任务队列、并发限制、取消与恢复标记。
- 实现 JSON/CSV 清单和配置快照。
- 实现 SSE 进度推送。

交付判断：批量任务单条失败不拖垮整批，服务重启后状态可解释。

### 阶段五：前端

- 配置列表与编辑。
- 将利益点编辑改为可添加、删除和排序的段列表。
- 素材表格、权重和概率显示。
- 素材表格展示文件日期，并支持素材名称、文件日期、权重、组内占比和状态的升降序排序。
- 素材选择与启用状态分离，支持当前素材库全选、Shift 连续选择和清除选择。
- 任务创建、计划预览、进度和结果页面。
- 完成基础易用性和错误提示。
- 完成 PR 式时间标尺栏、片段轨道和贯穿式播放头，并与视频播放器保持帧级同步。
- 完成基于时间线视口坐标的跟手拖动、缩放后逐帧定位和按住 Shift 才启用的吸附反馈。

交付判断：日常任务无需操作命令行即可完成。

### 阶段六：真实素材验收与打包

- 使用外接硬盘上的真实素材做受控小批量测试。
- 验证长路径、中文路径、磁盘拔出、空间不足和取消任务。
- 提供本机启动脚本、安装说明和故障排查文档。

### 阶段七：标准视频库与批量切片入库

- 实现本机目录选择、建库预检和标准目录的原子创建。
- 实现 `.smartstitch/library.json` 的读写、版本校验和库健康检查。
- 新建视频库时生成指向标准目录的 schema 2 配置。
- 将“添加利益点”改为后端幂等操作，同时建目录、修改时间线、修改素材组和递增库编号。
- 实现审核片段的批量切割、分类入库、`.part` 提交和切片清单。
- 保留外部素材配置的扫描与生成兼容性，不自动搬迁旧目录。

交付判断：用户选择一次保存位置后即可获得完整视频库，后续利益点段和批量切片均可自动落入正确目录并可追溯。

## 2. 测试矩阵

### 2.1 配置测试

| 场景 | 期望结果 |
|---|---|
| 合法完整配置 | 校验通过 |
| 负权重 | 指明字段并拒绝 |
| 必需组权重总和为 0 | 拒绝启动 |
| 相对路径 | 按定义的基准目录正确解析 |
| 外接卷未挂载 | 明确提示，不创建替代目录 |
| 配置保存中断 | 原配置仍可读取 |
| schema 1 单利益点配置 | 加载为 `benefit_1`，不立即覆盖原文件 |
| 删除中间利益点段 | 允许稳定编号不连续，时间线顺序正确 |
| 删除最后一个启用利益点段 | 拒绝保存并说明至少需要一段 |
| `timeline` 引用不存在的 `benefit_<n>` | 指明标识并拒绝 |
| 利益点段超过 20 个 | 拒绝并提示上限 |
| 旧配置省略 `visual_dedup` | 默认关闭且其余字段正常加载 |
| 视觉去重参数越界 | 指明 `scale/sigma/steps/brightness/opacity` 对应字段并拒绝 |

### 2.2 扫描测试

| 场景 | 期望结果 |
|---|---|
| `._视频.mp4` | 忽略 |
| 大写 `.MP4` | 正常识别 |
| 文件无视频流 | 标记无效 |
| 视频无音轨 | 允许并在渲染时补静音 |
| VFR 视频 | 标记并标准化为目标帧率 |
| 中文和空格路径 | 正常探测 |
| 扫描期间文件被删除 | 报告具体文件，不崩溃 |
| `qtrle` MOV 含透明像素 | 识别为可用动态边框 |
| MOV 非 `qtrle`、无 Alpha 或抽样全不透明 | 指明原因并阻止 required 模式生成 |
| exact 边框尺寸与输出画布不符 | 同时报告素材尺寸和目标尺寸 |
| 多个项目扫描全局边框库 | 得到相同资产 ID，不要求项目内存在边框副本 |
| 重复上传相同内容 | 按内容哈希返回已有资产，不产生重复文件 |
| 全局库版本冲突 | 返回 409，不覆盖另一台电脑的修改 |

### 2.3 权重测试

| 场景 | 期望结果 |
|---|---|
| 权重 6/3/1，数量 10，配额模式 | 次数 6/3/1 |
| 权重 6/3/1，数量 7，配额模式 | 总和为 7，按最大余数稳定分配 |
| 固定种子重复执行 | 计划一致 |
| 不同种子 | 次数配额相同但顺序通常不同 |
| strict 去重且数量超组合空间 | 创建任务失败并提示最大数量 |
| 产品标签不匹配 | 按配置报错或退回通用池 |
| 三个利益点段各有多个候选 | 每段独立按权重分配，每条计划按时间线含三段 |
| strict 去重与三个利益点段 | 最大组合数包含三个素材池的乘积 |

### 2.3.1 素材权重表交互测试

| 场景 | 期望结果 |
|---|---|
| 首次进入素材权重页 | 默认按素材名称升序展示，激活表头显示向上箭头 |
| 连续点击同一可排序表头 | 在升序和降序之间切换，箭头与实际顺序一致 |
| 点击另一个可排序表头 | 新字段从升序开始，原字段不再显示激活状态 |
| 按文件日期升序或降序 | 使用 `modified_at` 排列；无日期项稳定放在有日期项之后 |
| 按权重或组内占比排序后修改某行 | 修改按素材 ID 写回正确素材，不受当前行号影响 |
| 勾选一行素材 | 仅改变选择高亮和已选数量，不改变素材启用状态 |
| 点击“已启用 / 已停用”按钮 | 切换单个素材启用状态，不改变该行选择状态 |
| 已停用素材 | 状态按钮显示主题红色，且文字仍明确显示“已停用” |
| 表头全选 | 选中当前素材库全部素材；再次取消时清空当前素材库选择 |
| 部分素材已选择 | 表头选择框显示半选状态 |
| 选择一行后 Shift 选择另一行 | 当前排序下两行之间的素材全部进入相同选择状态 |
| 从表格非控件区域拖拽 | 显示浅绿色半透明选区，与选区相交的素材行被选中 |
| 从复选框、按钮或输入框开始拖拽 | 不启动框选，原控件保持正常交互 |
| 从未选素材开始向上或向下框选 | 本次命中的素材全部选中，并与原有选择合并 |
| 从已选素材开始框选 | 本次命中的素材全部取消选择，其他未命中素材保持原状态 |
| 同一范围连续框选两次 | 第二次完成后恢复到第一次框选前的选择状态 |
| 选中多条素材，在其中一行输入权重并按回车 | 当前素材库的已选素材全部改为输入值，未选素材和其他素材库不变 |
| 在未选行输入权重并按回车 | 只修改当前素材，不影响页面中已选素材 |
| 批量权重输入 `0` 并回车 | 允许应用，选中素材保留但不再参与抽取 |
| 权重为空、负数或非数字时回车 | 阻止修改并提示输入大于等于 0 的数 |
| 修改权重后尚未保存 | 只更新页面草稿，不写入配置；点击“保存权重”后统一提交 |
| 点击已选行的启用状态按钮 | 全部已选素材统一切换为被点击按钮的目标状态，未选素材不变 |
| 点击未选行的启用状态按钮 | 只切换被点击素材，不受页面中其他选择影响 |
| 进入素材权重页 | 获取配置级独占锁并持续续租，保存按钮在锁获取成功后可用 |
| 配置正被其他用户编辑 | 素材权重页保持只读并显示占用状态，不允许保存或修改 |
| 存在未保存修改时离开页面 | 询问是否放弃；取消时留在当前页，确认后释放独占锁 |
| 保存期间锁或配置版本失效 | 拒绝覆盖并显示错误，不以临时抢锁方式重试 |
| 素材权重页持锁达到 5 分钟 | 即使续租正常也主动释放锁、页面变为只读并弹窗提示 |
| 5 分钟内保存或持续操作 | 不重置硬上限，仍从首次取得锁的时间计算 |
| 配置管理页面持锁达到 5 分钟 | 即使续租正常也主动释放锁、关闭编辑弹窗、放弃未保存草稿并提示 |
| 切换素材库标签 | 各素材库分别保存临时选择状态，Shift 连选起点重置 |
| 重新扫描后素材被删除 | 自动移除已经不存在的素材 ID，不计入已选数量 |
| 点击清除选择 | 只清空当前素材库选择，不修改权重、标签或启用状态 |
| 条件筛选入口 | 当前版本不显示日期范围、标签或关键字筛选条件 |

### 2.4 渲染测试

| 场景 | 期望结果 |
|---|---|
| 全部输入规格一致 | 正常成片 |
| 分辨率、帧率不同 | 统一为输出规格 |
| 某片段无音频 | 成片时间线和音频连续 |
| 透明 PNG | 透明正确且位于最高层 |
| 全部利益点段叠图 | 从实际第一段开始到最后一段结束，起止由累计时长计算 |
| 时间线删减一个利益点段 | FFmpeg 输入、concat 顺序和预计时长同步减少该段 |
| 静态尾帧 1.5 秒 | 输出末尾持续约 1.5 秒 |
| FFmpeg 中途失败 | 无正式半成品，记录失败 |
| 输出重名 | 按冲突策略处理 |
| 视觉去重总开关关闭、子功能仍有选择 | 保留子功能配置，但不生成模糊背景或透明边框 |
| 开启模糊背景但关闭边框 | 仅生成模糊背景和缩小前景 |
| 关闭模糊背景但开启边框 | 主画面不缩小、不模糊，仅叠加透明边框 |
| 同时开启模糊背景与边框 | 按模糊背景、缩小前景、透明边框、风险提示语的顺序生成 |
| 短 `qtrle` 边框叠加长主视频 | 从第 0 帧循环且输出时长由主时间线决定 |
| 边框与风险提示语重叠 | 重叠像素显示风险提示语，证明其位于最高层 |
| 模糊背景与透明边框都关闭 | 现有 concat、风险提示语和音频过滤链保持兼容 |
| 全局随机边框使用固定种子 | 每条计划选择结果可复现并写入清单 |
| 固定边框被停用或尺寸不兼容 | 创建计划前阻止并指出素材 |
| 任务创建后全局素材被停用 | 已冻结任务仍使用原内容，新任务不再选择 |

### 2.5 任务测试

| 场景 | 期望结果 |
|---|---|
| 10 条中 1 条失败 | 其余继续，批次为 partial_failed |
| 用户取消 | 待执行项取消，临时文件安全处理 |
| 服务意外退出 | 重启后运行中任务标为 interrupted |
| SSE 断线重连 | 前端能恢复真实状态 |
| 磁盘空间跌破阈值 | 不启动新条目并给出告警 |

### 2.6 时间线审核测试

| 场景 | 期望结果 |
|---|---|
| 视频正常播放 | 时间标尺手柄和贯穿轨道的播放头竖线跟随实际呈现帧 |
| 视频时间停在某帧后半段 | 回退同步仍显示当前画面帧，不因四舍五入提前到下一帧 |
| 手动定位相邻帧或小于四分之一帧的时间差 | 播放器仍精确 seek 到目标帧，不保留旧画面 |
| 真实素材在断点帧起点仍呈现前一帧 | 预览 seek 使用目标帧中点，画面显示后一段首帧；断点权威帧号和时间码不偏移 |
| 拖动播放器进度条 | 时间线播放头、帧号和时间码同步到同一帧 |
| 时间标尺与轨道水平滚动 | 刻度、断点、片段边界和播放头保持横向对齐 |
| 切换多档缩放 | 主次刻度密度自适应，时间码标签不重叠 |
| 时间线页任意非输入区域按空格 | 无论鼠标位置，视频均在播放和暂停之间切换，页面不发生滚动；长按不重复切换 |
| 时间线页尚未完成分析时按空格 | 页面不发生滚动，不尝试播放 |
| 选中人工断点后再点击上一帧/下一帧 | 仅从当前播放头前后移动 1 帧，不移动所选断点 |
| 完成断点或待切片编辑后按 `Command + Z` | 撤销最近一次实质编辑，断点、分类、合并和片段列表恢复一致 |
| 撤销后按 `Command + Shift + Z` | 重做被撤销的编辑；撤销后如有新编辑，原重做历史失效 |
| 输入框中按撤销/重做快捷键 | 保留浏览器原生文本撤销/重做，不改变时间线历史 |
| 按住 Option 在时间线上滚轮滚动 | 以鼠标所在时间点为锚点平滑放大或缩小时间线 |
| 未按 Option 使用滚轮 | 不触发缩放，保留时间线正常滚动行为 |
| 单击或拖动时间标尺 | 播放头与鼠标横坐标同步，不出现固定比例减速或累计偏移 |
| 单击、双击或拖动下方轨道空白处 | 播放头和断点均不变化 |
| 拖动下方轨道中的断点 | 只调整该断点；播放头竖线、视频画面和当前播放帧保持不变 |
| 快速连续拖动 | 界面按最新指针位置刷新，无明显事件积压和松手后追赶 |
| 默认拖动经过断点 | 不发生吸附，播放头继续跟随鼠标 |
| 按住 Shift 进入断点 8 px 范围 | 吸附到目标帧，并高亮目标断点和时间码 |
| 按住 Shift 将断点拖入播放头 8 px 范围 | 断点吸附到播放头所在帧，播放头竖线、视频画面和当前播放帧不变化 |
| Shift 吸附后在 8～12 px 范围抖动 | 保持吸附，不在自由位置与目标间反复切换 |
| 拖动中松开 Shift | 立即解除吸附并回到鼠标对应位置，不发生跳回 |
| 多个吸附目标距离相同 | 按播放头、人工点、机器点、较小帧序号的顺序稳定选择 |
| 拖动已有断点到另一断点 | 不创建重复断点，按交互规则选中或保留已有点 |
| 拖动过程中松开到窗口外 | 正常结束拖动并恢复一致状态，不遗留捕获或暂停状态 |
| 视频播放中移动时间标尺播放头 | 移动时暂停，松开后仍保持暂停，不自动恢复 |
| 视频播放中拖动断点 | 拖动期间暂停；断点提交后恢复原播放状态，播放头位置不被断点改写 |
| 保存后重新加载 | 断点整数帧、顺序和片段范围保持不变 |
| 放大到单帧至少 4 px | 25/30/50/60 fps CFR 视频中可稳定选择相邻帧 |
| VFR 视频 | 明确标记近似帧率限制；不得静默声称达到 CFR 同等精度 |
| 有声视频完成分析 | V1 下方显示 A1 波形，与标尺、断点和播放头使用同一时间坐标 |
| 水平滚动或切换缩放 | A1 只渲染可见区波形，不使用超宽 Canvas，不与 V1 产生水平偏移 |
| 连续拖动缩放条 | 波形按绝对帧坐标即时展开或压缩，不保持铺满窗口的固定形状；请求返回后不跳位 |
| 同时存在画面与语音机器候选 | 画面候选只在 V1、语音候选只在 A1；人工调整后才显示贯穿式最终断点 |
| A1 默认锁定 | 片段选择、吸附和审核保存忽略纯音频候选，但共同命中点与人工断点仍有效 |
| 解锁 A1 | 纯音频候选恢复交互并参与片段划分；切换锁状态后失效选择被安全清理 |
| A1 锁定后切片 | 输出仍保留源视频同步音频，不把轨道锁误实现为静音 |
| 新前端连接未重启的旧后端 | 显示后端版本过旧提示，不将缺少 `audio` 字段误报为无音轨 |
| 无音轨或波形生成失败 | 显示文字状态，画面断点、人工审核和切片仍可用 |
| `5.0s 有声 + 0.6s 静音 + 有声` | `speech_pause` 候选约落在 5.12s，不落在下一段声音开始的 5.6s |
| 持续背景音乐使 `silencedetect` 返回零区间 | 使用自适应 RMS 回退寻找相对低能量停顿，并在分析记录中保留检测方法和阈值 |
| 静音短于“最短语音停顿” | 不生成 `speech_pause` 候选 |
| 音频停顿与转场间距不超过 0.18s | 合并为一点，优先转场帧并保留完整静音区间证据 |
| 加载 v1 `silence_end` 分析记录 | 保持只读兼容，显示原有原因，不原地重算或覆盖人工审核 |

### 2.7 标准视频库测试

| 场景 | 期望结果 |
|---|---|
| 未选保存位置 | 新建按钮不可用，不写盘 |
| 取消目录选择器 | 保持原表单，不创建配置或文件夹 |
| 中文与空格路径 | 创建完整目录树，配置可正确扫描 |
| 同名最终目录已存在 | 返回冲突，不合并且不改写已有内容 |
| 保存位置只读 | 预检拦截并显示具体路径 |
| 外接卷未挂载 | 报错且不在其他位置创建替代目录 |
| 标准建库成功 | 全部必需目录、库标记和 schema 2 配置一致 |
| 建库中任一步失败 | 不留下可被误认为成功库的半成品 |
| 同一建库请求重试 | 返回同一结果，不创建重复库 |
| 受管库添加利益点 | 目录、`sources`、`timeline` 和递增编号同步成功 |
| 连续或并发添加利益点 | 每个请求获得唯一且单调递增的编号 |
| 利益点目标目录已有视频 | 保留全部内容并关联目录，不覆盖 |
| 删除利益点后再添加 | 原目录保留，新段使用从未使用的更大编号 |
| 库内目标是越界符号链接 | 拒绝写入并指明安全原因 |
| 外部素材旧配置 | 照常扫描和生成，不自动创建整套标准目录 |
| 批量切片成功 | 视频进入目标素材池，清单记录源指纹、帧区间和输出路径 |
| 批量切片中单条失败 | 其他条目继续，无正式半成品，清单保留错误 |
| 断点列表改为片段选择 | `N` 个断点生成 `N + 1` 个可点击区间；默认列表为空，连续点击后累计添加且不重复 |
| 批量选择类型 | 同一列表内多条可分别选择前贴、引子、动态利益点、结尾、尾帧和未归类 |
| 调整断点后分类安全性 | 只保留帧区间完全未变的分类，受影响片段重置为待分类 |
| 单列表批量操作 | 不显示第二套批量区域；支持从片段列表移除误选条目 |
| 未标记任何片段 | 片段列表为空并阻止切片；未选择的其他片段不影响列表子集执行 |
| 切片审核版本过期 | 返回冲突且不启动 FFmpeg，不使用界面草稿切割 |

## 3. 真实素材验收建议

开发完成后按以下顺序验证，避免一开始就生成大量文件：

1. 只扫描当前目录并保存预检报告。
2. 固定种子生成 3 条计划，不渲染。
3. 渲染 1 条到临时输出目录，人工查看顺序和叠图。
4. 渲染 3 条，核对清单与实际内容。
5. 设置明显权重差异，生成 10 条并核对次数。
6. 人工制造一个无效素材，验证单条失败行为。
7. 最后再进行实际每日批量规模的压力测试。

## 4. 性能与容量

- 记录单条视频的渲染耗时、CPU 使用和输出大小，据此决定默认并发数。
- 配额计划生成应在千条规模下近乎即时。
- ffprobe 扫描按[《ffprobe 扫描性能优化设计》](./19-ffprobe扫描性能优化设计.md)实现持久化指纹缓存、未命中项有界并发和单文件超时；指定外接盘基准中冷扫描目标不超过 5 秒、暖扫描不超过 1 秒，CI 验证调用次数和并发上限而不验证真实秒数。
- 日志应轮转或按批次存放，防止长期使用占满磁盘。

## 5. 开发前待确认事项

以下事项不阻碍文档设计，但写代码前最好确认：

1. 风险提示语图片默认覆盖“整条成片”还是“全部利益点段”。当前 schema 2 示例采用“全部利益点段”。
2. 前贴是每条必需、可选随机，还是只对部分配置启用。当前示例采用“可选”。
3. 尾帧是静态图片、视频，还是两者都有。设计已兼容两者。
4. 产品前贴与风险提示语图片是否必须同产品匹配。当前设计默认启用产品标签匹配。
5. 已确认一条成片可包含多个利益点段，并由 schema 2 配置增删和排序；开发时按[《多段利益点配置设计》](./07-多段利益点配置设计.md)验收。
6. 每日常见生成数量和可接受耗时，用于确定默认并发和质量参数。
7. “淘宝闪购-常规”的实际素材、风险提示语图片和尾帧目录。
8. 成片是否需要限定总时长，或允许由所选片段自然相加。

## 6. 完成定义

只有同时满足以下条件，第一版才算完成：

- 文档中的核心验收标准全部通过。
- 自动化测试通过，真实素材小批量验收通过。
- 无需修改 Python 代码即可通过配置新增“淘宝闪购-常规”等场景。
- 任一输出视频都能追溯到输入素材、权重、随机种子和配置快照。
- 用户可仅通过网页完成配置、预览、生成、查看进度和定位结果。
- 视觉去重的简单模式只暴露强度预设和边框选择，高级模式承载精确参数，二者复用同一份配置草稿与保存链路。
